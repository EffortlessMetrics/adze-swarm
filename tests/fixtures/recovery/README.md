# Recovery Fixtures

Recovery fixtures prove structured diagnostics and document error facts for bad
input. They should cover byte spans, point ranges, UTF-8 accounting, multiline
excerpts, expected tokens, missing nodes, and `has_error` agreement where a
Tree-sitter-compatible selected tree exists.
