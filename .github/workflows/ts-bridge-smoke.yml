name: ts-bridge-smoke

on:
  push:
    branches: [main]
    paths:
      - tools/ts-bridge/**
      - scripts/smoke-link.sh
      - .github/workflows/ts-bridge-smoke.yml
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, labeled]
    paths:
      - tools/ts-bridge/**
      - scripts/smoke-link.sh
      - .github/workflows/ts-bridge-smoke.yml
  workflow_dispatch:
    inputs:
      matrix:
        description: "Runner matrix to use"
        type: choice
        default: linux
        options:
          - linux
          - full-os

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  matrix-setup:
    name: ts-bridge matrix setup
    if: |
      github.event_name != 'pull_request' ||
      github.event.action != 'labeled' ||
      github.event.label.name == 'full-ci' ||
      github.event.label.name == 'platform-matrix'
    runs-on: ubuntu-latest
    timeout-minutes: 2
    outputs:
      os: ${{ steps.compute.outputs.os }}
    steps:
      - id: compute
        run: |
          set -euo pipefail
          full=false
          if [ "${{ github.event_name }}" = "workflow_dispatch" ] && [ "${{ inputs.matrix }}" = "full-os" ]; then
            full=true
          elif [ "${{ github.event_name }}" = "pull_request" ] && \
            { [ "${{ contains(github.event.pull_request.labels.*.name, 'full-ci') }}" = "true" ] || \
              [ "${{ contains(github.event.pull_request.labels.*.name, 'platform-matrix') }}" = "true" ]; }; then
            full=true
          fi

          if [ "$full" = "true" ]; then
            echo 'os=["ubuntu-latest","macos-latest","windows-latest"]' >> "$GITHUB_OUTPUT"
          else
            echo 'os=["ubuntu-latest"]' >> "$GITHUB_OUTPUT"
          fi
          echo "full_os=$full"

  smoke:
    needs: matrix-setup
    strategy:
      matrix:
        os: ${{ fromJson(needs.matrix-setup.outputs.os) }}
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6.0.2
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          save-if: ${{ github.ref == 'refs/heads/main' }}
      
      - name: Install Tree-sitter libraries (Ubuntu)
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y libtree-sitter-dev pkg-config
      
      - name: Install Tree-sitter libraries (macOS)
        if: matrix.os == 'macos-latest'
        run: |
          brew install tree-sitter pkg-config
      
      - name: Install Tree-sitter libraries (Windows)
        if: matrix.os == 'windows-latest'
        run: |
          # Use vcpkg to install tree-sitter (if available)
          # or fallback to vendored build (default feature)
          echo "Using vendored Tree-sitter build on Windows"
      - name: Run ts-bridge smoke test
        shell: bash
        run: bash scripts/smoke-link.sh ts-bridge
