id = "adze-cli-dynamic-parse-boundary"
title = "Adze CLI Dynamic Parse Boundary"
status = "complete"
owner = "cli/product"
created = "2026-05-21"
archived = "2026-05-21"

objective = """
Make the feature-gated `adze parse --dynamic` surface safer without changing
release claims: prove the current dynamic parse boundary, keep dynamic output
below Stabilizing until behavior receipts exist, and preserve crates.io install
and release/publish blockers.
"""

handoff = """
Release and crates.io install work remains blocked on explicit human
authorization tracked by EffortlessMetrics/adze-swarm#325. This completed goal
closed the non-release dynamic CLI parse boundary hardening lane in
adze-swarm.
"""

end_state = [
  "adze-swarm remains the operating repo for CLI implementation and proof work.",
  "public adze remains the release, public-intake, promotion, tag, publish, signing, and Cargo-token surface.",
  "No release tag, crate publish, signing, Cargo-token, or crates.io install-receipt work happened in this lane.",
  "Dynamic parse output remains explicitly experimental and unimplemented unless behavior receipts prove otherwise.",
  "Dynamic-loading docs and support tiers do not imply a supported parse-output path.",
  "Stable CLI/WASM schema claims are not promoted without proof receipts and source-of-truth updates.",
]

[[work_item]]
id = "cli-dynamic-parse-source-of-truth"
status = "complete"
proposal = "docs/proposals/ADZE-PROP-0016-cli-dynamic-parse-boundary.md"
spec = "ADZE-SPEC-0011"
adr = "ADZE-ADR-0001"
plan = "plans/cli-dynamic-parse/implementation-plan.md"
issues = [325]
prs = [471]
commands = [
  "cargo run -q -p xtask -- check-active-goal --mode blocking",
  "cargo run -q -p xtask -- check-doc-artifacts --mode blocking",
  "git diff --check",
]

[[work_item]]
id = "dynamic-cli-boundary-receipts"
status = "complete"
proposal = "docs/proposals/ADZE-PROP-0016-cli-dynamic-parse-boundary.md"
spec = "ADZE-SPEC-0008"
adr = "ADZE-ADR-0001"
plan = "plans/cli-dynamic-parse/implementation-plan.md"
blocked_by = ["cli-dynamic-parse-source-of-truth"]
prs = [472]
commands = [
  "cargo test -p adze-cli test_parse_dynamic_without_feature_reports_feature_gate -- --exact --nocapture",
  "cargo test -p adze-cli --features dynamic dynamic -- --nocapture",
  "cargo fmt -p adze-cli -- --check",
  "cargo clippy -p adze-cli --all-targets --features dynamic -- -D warnings",
  "git diff --check",
]

[[work_item]]
id = "cli-dynamic-parse-closeout"
status = "complete"
proposal = "docs/proposals/ADZE-PROP-0016-cli-dynamic-parse-boundary.md"
spec = "ADZE-SPEC-0011"
adr = "ADZE-ADR-0001"
plan = "plans/cli-dynamic-parse/implementation-plan.md"
blocked_by = ["dynamic-cli-boundary-receipts"]
issues = [325]
prs = [473]
commands = [
  "cargo run -q -p xtask -- check-active-goal --mode blocking",
  "cargo run -q -p xtask -- check-doc-artifacts --mode blocking",
  "git diff --check",
]
